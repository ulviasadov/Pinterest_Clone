@{
ViewData["Title"] = "Admin Panel";
var users = ViewBag.Users as List<PinterestClone.Models.User>;
    var pins = ViewBag.Pins as List<PinterestClone.Models.Pin>;
        var boards = ViewBag.Boards as List<PinterestClone.Models.Board>;
            }
            <div class="container mt-4">
                <h2>Admin Panel</h2>
                <hr />
                <div class="mb-3">
                    <a asp-action="AdminAddUser" class="btn btn-success">Yeni Kullanıcı Ekle</a>
                    <form method="get" asp-action="AdminPanel" class="d-inline-block ms-3">
                        <input type="text" name="userSearch" placeholder="Kullanıcı ara..."
                            class="form-control d-inline-block w-auto" style="display:inline-block;width:200px;" />
                        <button type="submit" class="btn btn-outline-primary btn-sm">Ara</button>
                    </form>
                </div>
                <h4>Kullanıcılar</h4>
                <table class="table table-bordered">
                    <thead>
                        <tr>
                            <th>Id</th>
                            <th>Ad</th>
                            <th>Email</th>
                            <th>Admin</th>
                            <th>İşlem</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var user in users)
                        {
                        <tr>
                            <td>@user.Id</td>
                            <td>@user.Name</td>
                            <td>@user.Email</td>
                            <td>@(user.IsAdmin ? "Evet" : "Hayır")</td>
                            <td>
                                @if (!user.IsAdmin)
                                {
                                <a asp-action="AdminEditUser" asp-route-id="@user.Id"
                                    class="btn btn-warning btn-sm">Düzenle</a>
                                <a asp-action="DeleteUser" asp-route-id="@user.Id" class="btn btn-danger btn-sm">Sil</a>
                                }
                            </td>
                        </tr>
                        }
                    </tbody>
                </table>
                <h4>Pinler</h4>
                <table class="table table-bordered">
                    <thead>
                        <tr>
                            <th>Id</th>
                            <th>Başlık</th>
                            <th>Sahip</th>
                            <th>İşlem</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var pin in pins)
                        {
                        <tr>
                            <td>@pin.Id</td>
                            <td>@pin.Title</td>
                            <td>@pin.UserId</td>
                            <td>
                                <a asp-action="DeletePin" asp-route-id="@pin.Id" class="btn btn-danger btn-sm">Sil</a>
                            </td>
                        </tr>
                        }
                    </tbody>
                </table>
                <h4>Panolar</h4>
                <table class="table table-bordered">
                    <thead>
                        <tr>
                            <th>Id</th>
                            <th>Başlık</th>
                            <th>Sahip</th>
                            <th>İşlem</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var board in boards)
                        {
                        <tr>
                            <td>@board.Id</td>
                            <td>@board.Title</td>
                            <td>@board.UserId</td>
                            <td>
                                <a asp-action="DeleteBoard" asp-route-id="@board.Id"
                                    class="btn btn-danger btn-sm">Sil</a>
                            </td>
                        </tr>
                        }
                    </tbody>
                </table>
            </div>